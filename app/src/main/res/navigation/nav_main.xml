<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/history_fragment">

    <fragment
        android:id="@+id/history_fragment"
        android:name="com.nvkhang96.trackme.HistoryFragment"
        tools:layout="@layout/fragment_history">

        <action
            android:id="@+id/action_history_fragment_to_record_fragment"
            app:destination="@id/record_fragment" />
    </fragment>

    <fragment
        android:id="@+id/record_fragment"
        android:name="com.nvkhang96.trackme.RecordFragment"
        tools:layout="@layout/fragment_record"/>

</navigation>